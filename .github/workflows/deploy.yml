script: |
  cd /home/ubuntu

  if [ ! -d "Tracker" ]; then
    git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
  fi

  cd YOUR_REPO
  git pull

  docker stop finance-app || true
  docker rm finance-app || true

  docker build -t finance-app .
  docker run -d -p 8501:8501 --name finance-app finance-app
